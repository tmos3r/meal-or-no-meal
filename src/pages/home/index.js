import styles from "./index.module.css";

const Home = () => {
  return (
    <div className={styles.homeText}>
      Select an option in the menu to get started!
    </div>
  );
};

export default Home;
